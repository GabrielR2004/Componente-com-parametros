import React from "react";

export default function ComponenteParametro (props) {
    return (
        <div id="app">
            <h1>{props.titulo}</h1>
            <h2>{props.subtitulo}</h2>

        </div>
    )
    
}